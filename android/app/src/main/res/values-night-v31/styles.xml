<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ system splash screen customization (dark mode) -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/app_background</item>

        <!-- Keep the system splash animation effectively instant -->
        <item name="android:windowSplashScreenAnimationDuration">10</item>

        <!-- Keep system splash minimal; we render the quote as a native overlay view -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_transparent</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="android:windowSplashScreenBrandingImage">@null</item>

        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
</resources>
